export const add = (a, b) => a + b;
export const sub = (a, b) => a - b;

// export default function (a, b) {
//   return a * b;
// }

const div = (a, b) => a / b;

// export { div };
export default div;
